'use client'

import { useState, useEffect } from "react";


//state para la data
//loading
//useefect con el axios
//rsponse.data
//en la url concatenar el id
//hacer el setrecipe
//sr trycatch
//en el return mostrar la image de la comida

const RecipeContainer = ({ id }) => {
  return (
    <div>RecipeContainer {id}</div>
  );
};

export default RecipeContainer
